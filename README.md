A database college_2 is created.
It is used while creating tables Department, student_1, Course_1, Enrollment_1.
A table named Department is created which uses attributes department_id and department_name. 
Another table named student_1 is created which uses attributes roll_no, name, email_id, aadhar_no and Department_id as a foreign key which referes to table Department.
Another table named Course_1 is created which uses attributes course_1_name, course_1_id and Department_id as a foreign key which referes to table Department.
Another table named Enrollment_1 is created which uses attributes roll_no, course_1_id and sem. roll_no and course_1_id are foreign keys which refers to tables Student_1 and Course_1 respectively.
Multiple values are inserted in each table.
the code is executed.
